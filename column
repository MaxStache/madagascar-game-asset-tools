// THIS IS A DECOMPILATION AND MIGHT NOT BE 100% ACCURATE 

globals {
    "Script Time::value";
    "players::set::actor";
    "Dialogue Line Activate::message";
    "Dialogue Handler::actor";
}

locals {
    "PAUSE COUNTER::user::value";
    "SWITCH::user::value";
}

prescript {
    startup {
        createVariable(PAUSE COUNTER);
        // !! Unknown or Unimplemented opcode `OpSetValue`!
        createVariable(SWITCH);
    }
    
    shutdown {
    }
    
    update {
    }
}

behavior Wait and talk {
    // !! Unknown or Unimplemented opcode `OpCheckValue`!
}

behavior Timer {
    // !! Unknown or Unimplemented opcode `OpSetValue`!
    // !! Unknown or Unimplemented opcode `OpSetValue`!
    // !! Unknown or Unimplemented opcode `OpSetBehavior`!
}


