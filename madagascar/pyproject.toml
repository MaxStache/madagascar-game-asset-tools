[project]
name = "madagascar"
description = "Modding and file tools for - Madagascar the game, 2005 -"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["lib*", "sections*", "streamfuncs*", "stream*"]